extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action='/download', method='post')
      span Youtube URL:
      input(type='text', name='youtubeURL')
      input(type='submit', value='Download')
